schema_version = 1
target_id = "micro-crash-if-magic"
harness = "achlys_micro_crash_if_magic"
input_mode = "inprocess"
max_input_len = 64
timeout_ms = 200

[[sources]]
path = "benchmarks/micro/crash_if_magic.c"

[builds.fast]
instrumentation = "sancov-edge"
flags = ["-O0", "-g"]

[builds.canonical]
instrumentation = "sancov-edge"
flags = ["-O0", "-g", "-fsanitize-coverage=trace-pc-guard"]

[builds.sanitizer]
instrumentation = "none"
sanitizers = ["address", "undefined"]
flags = ["-O0", "-g", "-fsanitize=address,undefined"]
