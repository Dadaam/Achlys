# T2 evidence bundles

T2 is **not accepted**. Do not put a `T2_PASS` token or an AFL++
comparison here.

Linux x86-64 snapshot (baseline `fcb725c` + A/B `7b2a40c`):
[`2026-08-17-linux-x86_64/`](2026-08-17-linux-x86_64/).

That bundle records tables, manifests, and checksums only. Campaign
trees stay on the host under `/root/`. macOS smokes remain
`T2_LADDER=DEFINED`.
